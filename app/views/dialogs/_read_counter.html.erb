<%= turbo_frame_tag "#{dom_id(dialog)}_read_counter" do %>
  <% # it should on one line to apply :empty pseudo element styling %>
  <div class="read_counter <%= element_class %>"><%= dialog.unread_messages_count unless dialog.unread_messages_count.zero? %></div>
<% end %>
