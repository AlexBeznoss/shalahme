<% content_for :title, "Edit user" %>

<div class="max-w-md my-8 bg-white overflow-hidden shadow rounded-lg">
  <div class="px-4 py-5 sm:p-6">
    <%= turbo_frame_tag @user, class: "container" do %>
      <%= render "form", user: @user %>
    <% end %>
  </div>
</div>
